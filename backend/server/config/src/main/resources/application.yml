server:
  port: 8888

spring:
  application:
    name: config-server

  output:
    ansi:
      enabled: "always"


  cloud:
    config:
      server:
        git:
          uri: file:///Users/jayji/Desktop/testRepo
          default-label: master