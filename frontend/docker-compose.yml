services:
  frontend:
    build: .
    container_name: frontend
    ports:
      - "8167:80"
    volumes:
      - .:/app
      - /app/node_modules
    restart: always
    networks:
      - backend_studion-network

networks:
  backend_studion-network:
    external: true
